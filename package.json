{
  "name": "rihan-ai-ml",
  "version": "0.1.0",
  "description": "AI/ML slice: extraction schema, marker normalization, chat companion guardrails — ChronicCare AI (with Trendline)",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:all": "concurrently -n backend,frontend -c blue,magenta \"npm run dev\" \"npm run dev:frontend\"",
    "setup": "npm install && npm --prefix frontend install"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^9.9.2",
    "multer": "^2.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "nodemon": "^3.1.4"
  }
}
