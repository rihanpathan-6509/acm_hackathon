# Base URL for the ChronicCare AI backend's API. Defaults to
# http://localhost:4001/api if unset — override this once the backend is
# deployed somewhere other than localhost.
VITE_API_BASE_URL=http://localhost:4001/api
